using NUnit.Framework;

/*
    T: CookCtrl
    X: Display, Timer, PowerTube, Output
    S:
*/
namespace Microwave.Test.Integration
{
    public class Integration2
    {
        [SetUp]
        public void Setup()
        {
            /*_stubbeddisplay = Substitute.For<IDisplay>();
            _stubbedpowerTube = Substitute.For<IPowerTube>();
            
            _sut = new Timer();
            _cookController = new CookController(_sut,)
            */
        }

        [Test]
        public void Test1()
        {
            Assert.Pass();
        }
    }
}